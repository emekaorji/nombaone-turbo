/**
 * Re-export of the canonical `cn` from @nombaone/ui so app-side imports stay
 * short (`@/lib/cn`) without pulling the whole barrel. Implementation lives at
 * packages/ui/src/lib/utils.ts.
 */
export { cn } from '@nombaone/ui/lib/utils';
