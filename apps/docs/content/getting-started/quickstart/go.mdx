---
title: Go quickstart
description: "Reach a real 201 with Go: create your first plan on the Nomba One sandbox in a few lines."
section: get-started
---

Create your first plan on the Nomba One sandbox with **Go**. This is a real
call that returns a `201`; only the key (`nbo_sandbox_…`) and the host change when
you go live.

## 1 · Get a sandbox key

Grab a `nbo_sandbox_…` key. See [authentication](/getting-started/authentication).
Standard library only: `net/http`, no dependency. Read the key from `os.Getenv("NOMBAONE_API_KEY")`.

## 2 · Create a plan

<Snippet method="POST" path="/v1/plans" body={{ name: "Pro" }} idempotent defaultLang="go" />

A `201` comes back with the plan's `data.id`, its public reference. Amounts on
its [prices](/guides/create-plans-and-prices) are **integer kobo**.

## Next

<CardGroup cols={2}>
  <Card title="Your first subscription" href="/getting-started/your-first-subscription">
    The full plan → price → customer → subscription flow to a live 201.
  </Card>
  <Card title="Authentication" href="/getting-started/authentication">
    The secret-key rules and how the prefix pins the environment.
  </Card>
</CardGroup>
