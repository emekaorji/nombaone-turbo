/**
 * Central Drizzle schema barrel for the Nombaone infra database.
 * Drizzle-kit reads this file as the migration source of truth.
 */
import * as schema from './schema/index';

export * from './schema/index';

export { schema };
